'use client';

import { api } from '@/convex/_generated/api';
import { getAllData } from '@/convex/documents';
import { useQuery } from 'convex/react';
import React from 'react';

export default function BuyPage() {
  return <div>Buy</div>;
}
